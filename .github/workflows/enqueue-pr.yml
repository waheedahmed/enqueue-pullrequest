name: Enqueue Pull Request

on:
  pull_request:

jobs:
  enqueue-pullrequest:
    runs-on: ubuntu-latest
    permissions:
      pull-requests: write
      contents: read
    steps:
      - uses: actions/checkout@v4
      - uses: ./
        with:
          label: ""
